export enum Roles {
  "MANAGER" = "manager",
  "DEVELOPER" = "developer",
}

export enum ScoreTypes {
  technical_score = 1,
  problem_solving_score = 2,
  project_domain_score = 3,
  communication_score = 4,
  soft_skill_score = 5,
} 
